# Evidencias

Agregar aquí las capturas de pantalla del pipeline funcionando:

1. `n8n_ejecucion.png` — workflow ejecutado con todos los nodos en verde
2. `postgres_validados.png` — resultado de `SELECT * FROM solicitudes_validadas`
3. `postgres_rechazados.png` — resultado de `SELECT * FROM solicitudes_rechazadas`
4. `docker_logs.png` — salida de `docker logs solufin_n8n`
